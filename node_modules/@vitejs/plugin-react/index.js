// Temporary shim for @vitejs/plugin-react to allow build
export default function reactPlugin() {
  return {
    name: 'mock-react-plugin',
    transform(code, id) {
      return { code, map: null };
    }
  };
}
